# 这套"词汇打卡"到底是怎么搞出来的（大白话全解）

> 目标：哪怕你完全不懂电脑，读完也能明白——**这一套东西是干嘛的、由哪几块拼成、我是怎么把它做出来的、你每天怎么用**。

---

## 一、先用大白话讲：这一套是干嘛的

假设你有一本"好词好句笔记本"，里面记了 **686 个高级词汇**（比如"博大精深""锦上添花"），每个词还附了意思和例句。

你有个小愿望：

> 每在作文里用到一个词，就在它旁边记一笔"我用过一次"。这样复习时，一眼看出哪些词用熟了、哪些还没练过。

我帮你做的事，就是把这本笔记变成了一个**手机网页**：

- 网页上整整齐齐列着那 686 个词；
- 你点一下某个词，它就"叮"地记一次（次数 +1）；
- 点开还能看它的意思和例句，边打卡边复习；
- **你在手机上点的，电脑上也能看到**——因为记录存在"网上一个共享本子"里，不是只存在你这台手机。

就这么简单。下面把"它是怎么拼出来的"一层层拆开。

---

## 二、整套东西由三个零件组成

记不住名字没关系，记住它们扮演的"角色"就行：

| 零件 | 它扮演的角色 | 存什么 | 打个比方 |
|---|---|---|---|
| **你的词汇笔记**（源 md 文件） | 原材料，一直不动 | 686 个词 + 意思 + 例句 | 菜谱原本 |
| **打卡网页**（挂在 CloudStudio） | 你点着玩的界面 | 网页画面本身（不含记录） | 厨房（做菜的地方） |
| **云端账本**（Supabase） | 记"每个词打了几次"的地方 | 次数、时间、掌握度 | 记账的本子 |

一句话串起来：**词汇笔记是菜谱 → 网页是厨房（你操作的地方）→ 云端账本是本子（记你做了几道菜）。**

---

## 三、这套网页是怎么一步步搭出来的（详细步骤）

从"一本笔记"到"一个能用的网页"，总共走了 6 步。前 4 步是我（AI）干的，后 2 步需要你配合：

| 第几步 | 谁弄的 | 干了啥 | 大白话 |
|---|---|---|---|
| 1 | 我 | 用程序把你 md 笔记里的 686 个词和例句扒出来，变成网页能读的格式 | 把笔记本抄成"网页能懂的清单" |
| 2 | 我 | 写了一个网页画面：词排好队、点一下 +1、能展开看例句、能搜 | 做了个"点词计数器" |
| 3 | 我 | 写了"联网记账"的代码，让网页会去云端账本读写 | 给计数器接了根网线 |
| 4 | 我 | 把网页传到 CloudStudio，得到一个网址发给你 | 把计数器挂到网上，给你链接 |
| 5 | 你 | 在 Supabase 建了张表，把"网址+钥匙"发我 | 租了个网上的本子，把钥匙给我 |
| 6 | 我 | 把钥匙焊进网页、重新挂上网 | 计数器认得那个本子了，完工 |

> 你看，你真正要动手的只有第 5 步（建个表、复制两个值发我），其余都是我后台做的。

**第 5 步你具体做了什么**（复习用）：
1. 在 supabase.com 注册、新建一个项目 = 租了块云空间；
2. 在 SQL 页面粘贴几行、点 Run = 在云空间里建了张"打卡表"；
3. 复制 Project URL + publishable key 发我 = 给了"本子地址 + 开门钥匙"。

---

## 四、那张"打卡表"长什么样（建表 SQL 的 5 个格子）

云端账本里那张表，一行对应一个词（共 686 行），每行有 5 个格子：

| 格子（字段） | 记的东西 | 例子 |
|---|---|---|
| `id` | 词的编号（第几个词） | `5` |
| `count` | 你打卡的次数（开始是 0） | `3` |
| `first_used` | 第一次打卡的时间 | `2026-07-22 09:10` |
| `last_used` | 最近一次打卡的时间 | `2026-07-22 21:30` |
| `mastery` | 掌握程度（未用/偶尔/熟练） | `偶尔` |

> 注意：词本身的文字（意思、例句）**不在这张表里**，它们打包在网页里。云端表只记"打卡状态"——这样本子很薄、同步很快。

---

## 五、你点一下"打卡"，背后发生了什么

把"点一下"拆成慢动作：

1. 你点网页上某个词（比如"博大精深"）。
2. 网页知道它是第 5 号。
3. 网页给云端账本发话：**"第 5 行次数 +1，最近使用写成现在。"**
4. 云端账本把新数字写进本子。
5. 你（或另一台设备）刷新 → 网页把整本子拉回来 → 把数字和绿点显示出来。

> 打开网页那一瞬间，浏览器先拿"上次存在本机的数据"秒开页面（快），再偷偷从云端拉最新状态覆盖（准）。所以**没网也能打卡**，有网后自动补上。

---

## 六、为什么手机和电脑看到的是同一份

因为手机和电脑打开的**是同一个网页**，网页翻的**是同一个云端本子**：

- 手机把第 5 行改成 `count=3` → 写进云端；
- 电脑一刷新 → 从云端拿到第 5 行的新值 `3`。

**同步的本质 = 大家都翻同一本云本子，而不是两台手机互相抄。** 所以你什么都不用导、不用手动传。

### 网页自己的"小记事本"（双层存储）

你可能会问：网页自己不带存储吗？**带，但是临时的。**

网页有一份存在"你这台手机浏览器"里的小记事本（`localStorage`），没网也能打卡。但它有天花板：

| 情况 | 只靠这个小记事本会怎样 |
|---|---|
| 换手机 / 电脑 | 看不到另一台的数据 |
| 清了浏览器缓存 | 记录没了 |
| 同一台机换浏览器（Chrome→Safari） | 也看不到 |

所以才加了 Supabase 当"第二层、网上的本子"。现在两层配合：

- **第一层（手机自带小记事本）**：秒开、没网也能用；
- **第二层（Supabase 云端本子）**：跨设备同步、不怕清缓存。

一句话：**网页自己有个临时小本子（单机够用），但"换设备也不丢"靠的是云端那本。**

---

## 七、技术词大白话翻译表（看不懂就查这张）

| 技术词 | 大白话 |
|---|---|
| 网页 / 前端 | 你点着玩的那个屏幕界面 |
| CloudStudio | 把网页挂到网上、给你链接的地方（24 小时开着） |
| Supabase | 网上的一个共享本子（数据库），记打卡次数 |
| 数据库 / 表 | 就是一张大表格，一行一个词 |
| localStorage | 你手机浏览器自己的小记事本（换手机看不到） |
| RLS 策略 | 本子的门禁：谁能翻、谁能写 |
| publishable key | 打开本子的公开钥匙（本来就能给人看，写进网页很正常） |
| anon（匿名） | 不登录的陌生人 |
| last-write-wins | 后写的覆盖先写的（见下节"小毛病"） |

---

## 八、一个要知道的小毛病

同步规则是"**后写的覆盖先写的**"：

- 如果同一个词，你在手机和电脑**都断网**各打了一次，再联网——可能只算 1 次（后一次盖掉前一次），不是 2 次。
- 一个人正常使用基本碰不到（你不会同时两台设备离线打同一个词）。真要严丝合缝地累加，得加复杂逻辑，对个人打卡是杀鸡用牛刀。

---

## 九、三种玩法对比（数据都同源，随便挑）

| 玩法 | 链接 | 怎么打卡 | 怎么同步 |
|---|---|---|---|
| 网页 + 云端本子（已配通） | `https://996d3bb65bf24b66b3d2f30f5fdb9e53.app.codebuddy.work` | 点词即 +1，能看例句 | 云端自动同步，不用登录 |
| 腾讯多维表格 | `https://docs.qq.com/smartsheet/DSGdydFZOY3BnVGx5` | 手动把数字 +1 | 腾讯账号云端同步 |
| 网页单机版 | 同上面那个链接（不连云端） | 点词即 +1 | 只存在本机，靠导出文件迁移 |

> 三套之间用网页"设置 → 导出/导入"可以互导。词都来自你那本 `002-高频词汇库.md`。

---

### 复核小结（逻辑是否通顺）

全文按这个顺序讲，小学生也能顺着读下来：

1. **干嘛用**（把笔记变成点词计数器）→
2. **由哪三块组成**（笔记 / 网页 / 云端本子）→
3. **怎么造出来的**（6 步，你只动第 5 步）→
4. **表里记什么**（5 个格子）→
5. **点一下背后发生啥**（运行时）→
6. **为啥能同步 + 网页自带小本子**（双层存储）→
7. **术语翻译表**（卡壳就查）→
8. **小毛病** →
9. **三种玩法**。

每个术语首次出现都配了比方，没有"前面没解释过的词突然蹦出来"的跳跃。
